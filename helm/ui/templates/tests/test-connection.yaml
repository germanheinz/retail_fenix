apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "retail-fenix.fullname" . }}-test-connection"
  labels:
    {{- include "retail-fenix.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "retail-fenix.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
