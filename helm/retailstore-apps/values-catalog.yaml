# Catalog Custom Values
image:
  pullPolicy: Always

mysql:
  create: true 

app:
  persistence:
    provider: mysql
    endpoint: ""
    database: "catalog"

    secret:
      create: true
      name: catalog-db
      username: mydbadmin
      password: german123